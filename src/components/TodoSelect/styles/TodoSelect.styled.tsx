import styled from "styled-components";

export const TodoSelectStyled = styled.select`
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
  border: 1px solid #fff;
  outline: none;
  font-size: 1rem;
  font-weight: 500;
  cursor: pointer;
  margin: 0 0 1rem 0;
  display: flex;
  width: 100%;
  background-color: rgba(243, 24, 24, 0.3);
  color: #fff;
`

export const TodoOptionStyled = styled.option`
  color: #000;
  cursor: pointer;
  font-size: 1rem;
  font-weight: 500;
  background-color: rgba(243, 24, 24, 0.3);
`
